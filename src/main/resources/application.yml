spring:
  profiles:
#    active: prod
     active: dev
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/spboot
    username: root
    password: root
  jpa:
    hibernate:
      #ddl-auto: create #每次重启创建新表
      ddl-auto: update
    show-sql: true
